# Secrets — never commit
.env
.env.*
!.env.example
caddy/admin.hash

# Generated config (rendered from caddy/Caddyfile.tmpl)
caddy/Caddyfile

# Local backups
backups/
*.tar.gz

# Watchdog state
.watchdog.state

# OS / editor cruft
.DS_Store
*.swp
